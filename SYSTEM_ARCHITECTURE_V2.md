# TRADE-AI-ASSISTANT 시스템 구성도

## 🏗️ 서비스 아키텍처 (3-Tier)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              TRADE-AI-ASSISTANT                              │
├─────────────────────┬─────────────────────┬─────────────────────────────────┤
│                     │                     │                                 │
│   🌐 Frontend       │   ⚙️ Backend        │   🤖 AI Server (RunPod GPU)     │
│   (Vercel)          │   (AWS/Cloud)       │   (RunPod Serverless)           │
│                     │                     │                                 │
│   React + Vite      │   Django + DRF      │   FastAPI + AI Models           │
│   Port: 5173        │   Port: 8000        │   Port: 8000                    │
│                     │                     │                                 │
└─────────────────────┴─────────────────────┴─────────────────────────────────┘
         │                     │                          │
         │    REST API         │       REST API           │
         └─────────────────────┼──────────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │   External Services │
                    ├─────────────────────┤
                    │ • Qdrant (Vector DB)│
                    │ • OpenAI API        │
                    │ • Tavily (Web)      │
                    │ • AWS S3            │
                    │ • MySQL             │
                    └─────────────────────┘
```

---

## 📁 프로젝트 구조

```
TRADE-AI-ASSISTANT/

├─────────────────────────────────────────────────────────────────────────────
│ 🌐 SERVICE 1: Frontend (독립 배포)
├─────────────────────────────────────────────────────────────────────────────
│
├── frontend/                          # React 프론트엔드 (Vite + TypeScript)
│   │
│   ├── App.tsx                        # 메인 앱 컴포넌트
│   │
│   ├── src/
│   │   ├── main.tsx                   # 앱 진입점
│   │   └── index.css                  # 글로벌 CSS
│   │
│   ├── components/
│   │   │
│   │   ├── LoginPage.tsx              # 로그인 페이지
│   │   ├── MainPage.tsx               # 메인 페이지
│   │   ├── DocumentCreationPage.tsx   # 문서 생성 페이지
│   │   ├── StepSelector.tsx           # 단계 선택기
│   │   ├── PdfViewer.tsx              # PDF 뷰어
│   │   ├── OthersDocumentViewer.tsx   # 기타 문서 뷰어
│   │   ├── VersionHistorySidebar.tsx  # 버전 히스토리 사이드바
│   │   ├── ShootingStarIntro.tsx      # 인트로 애니메이션
│   │   │
│   │   ├── CommercialInvoiceTemplate.tsx   # Commercial Invoice 템플릿
│   │   ├── SalesContractTemplate.tsx       # Sales Contract 템플릿
│   │   │
│   │   ├── document-creation/         # 문서 생성 모듈
│   │   │   ├── hooks/                 # 커스텀 훅
│   │   │   ├── layout/                # 레이아웃 컴포넌트
│   │   │   ├── modals/                # 모달 컴포넌트
│   │   │   ├── steps/                 # 단계별 컴포넌트
│   │   │   └── types.ts               # 타입 정의
│   │   │
│   │   ├── ui/                        # UI 컴포넌트 (shadcn/ui)
│   │   └── figma/                     # Figma 관련 컴포넌트
│   │
│   ├── utils/                         # 유틸리티 함수
│   │   ├── api.ts                     # API 통신 모듈
│   │   ├── documentApi.ts             # 문서 API 모듈
│   │   └── documentUtils.ts           # 문서 유틸리티
│   │
│   ├── templates/                     # 문서 데이터 템플릿 (TS)
│   ├── doc_templates/                 # HTML 문서 템플릿
│   ├── styles/                        # 스타일
│   │
│   ├── package.json                   # 의존성 관리
│   ├── vite.config.ts                 # Vite 설정
│   └── .env                           # 환경 변수 (VITE_API_URL)



├─────────────────────────────────────────────────────────────────────────────
│ ⚙️ SERVICE 2: Backend (독립 배포)
├─────────────────────────────────────────────────────────────────────────────
│
├── backend/                           # Django 백엔드 API 서버
│   │
│   ├── manage.py                      # Django 관리 스크립트
│   │
│   ├── config/                        # Django 프로젝트 설정
│   │   ├── __init__.py                # PyMySQL 초기화
│   │   ├── settings.py                # Django 설정
│   │   ├── urls.py                    # 메인 URL 라우팅
│   │   ├── asgi.py                    # ASGI 설정
│   │   └── wsgi.py                    # WSGI 설정
│   │
│   ├── chat/                          # 채팅 API 앱
│   │   ├── models.py                  # 채팅 모델 (Conversation, Message)
│   │   ├── views.py                   # 채팅 뷰
│   │   ├── trade_views.py             # 무역 관련 뷰
│   │   ├── urls.py                    # 채팅 URL 라우팅
│   │   ├── serializers.py             # DRF 시리얼라이저
│   │   ├── memory_service.py          # 메모리 서비스 (Mem0)
│   │   └── config.py                  # 채팅 설정
│   │
│   ├── documents/                     # 문서 관리 앱
│   │   ├── models.py                  # 문서/사용자 모델
│   │   ├── views.py                   # 문서 뷰
│   │   ├── urls.py                    # 문서 URL 라우팅
│   │   ├── serializers.py             # DRF 시리얼라이저
│   │   └── services.py                # 문서 서비스 레이어
│   │
│   ├── agent_core/                    # AI 에이전트 모듈 (Backend 내 모듈)
│   │   │
│   │   ├── agents.py                  # 메인 에이전트 (OpenAI Agents SDK)
│   │   ├── config.py                  # 에이전트 설정
│   │   ├── utils.py                   # 유틸리티 함수
│   │   │
│   │   ├── models/                    # AI 모델 클라이언트
│   │   │   ├── query_transformer.py   # 쿼리 변환 모델
│   │   │   └── reranker.py            # 리랭커 클라이언트 (→ RunPod 호출)
│   │   │
│   │   ├── services/                  # 서비스 레이어
│   │   │   ├── query_transformer_service.py  # 쿼리 변환 서비스
│   │   │   └── reranker_service.py    # 리랭킹 서비스 (→ RunPod API 호출)
│   │   │
│   │   ├── tools/                     # 에이전트 도구
│   │   │   ├── search_tool.py         # RAG 검색 도구 (Qdrant)
│   │   │   └── web_search_tool.py     # 웹 검색 도구 (Tavily)
│   │   │
│   │   ├── prompts/                   # 프롬프트
│   │   │   ├── trade_instructions.txt # 무역 지시사항 프롬프트
│   │   │   └── fallback.py            # 폴백 프롬프트
│   │   │
│   │   ├── collection_manager.py      # Qdrant 컬렉션 관리
│   │   ├── pdf_parser.py              # PDF 파싱 (PyMuPDF)
│   │   ├── s3_utils.py                # AWS S3 유틸리티
│   │   └── langfuse_config.py         # Langfuse 설정 (LLM 모니터링)
│   │
│   ├── scripts/
│   │   └── setup_s3_cors.py           # S3 CORS 설정 스크립트
│   │
│   ├── requirements.txt               # Python 의존성
│   └── .env                           # 환경 변수 (RUNPOD_API_URL 포함)



├─────────────────────────────────────────────────────────────────────────────
│ 🤖 SERVICE 3: AI Server - RunPod GPU (독립 배포)
├─────────────────────────────────────────────────────────────────────────────
│
├── trade_ai_runpod/                   # RunPod GPU 서버
│   │
│   └── unified_api/                   # Reranker API 서버 (FastAPI)
│       │
│       ├── main.py                    # FastAPI 앱 진입점
│       ├── requirements.txt           # Python 의존성
│       ├── Dockerfile                 # Docker 설정
│       ├── test_api.py                # API 테스트 스크립트
│       │
│       ├── models/                    # Pydantic 모델 (Request/Response)
│       │   ├── __init__.py
│       │   └── reranker.py            # Reranker 스키마
│       │
│       ├── services/                  # 비즈니스 로직 & AI 모델
│       │   ├── __init__.py
│       │   └── reranker.py            # Reranker 서비스 (mxbai-rerank-large-v2)
│       │
│       └── routers/                   # API 엔드포인트
│           ├── __init__.py
│           └── reranker.py            # POST /rerank 엔드포인트


└── .gitignore                         # Git 제외 파일
```

---

## 🔗 서비스별 기술 스택

| 서비스 | 기술 | 배포 환경 |
|--------|------|-----------|
| **Frontend** | React, TypeScript, Vite, Tailwind CSS, shadcn/ui | Vercel |
| **Backend** | Django, Django REST Framework, OpenAI Agents SDK | AWS / Cloud |
| **AI Server** | FastAPI, mxbai-rerank-large-v2 | RunPod GPU |

### External Services
| 구분 | 기술 |
|------|------|
| **Vector DB** | Qdrant Cloud |
| **LLM** | OpenAI GPT-4o |
| **Memory** | Mem0 |
| **Web Search** | Tavily API |
| **Storage** | AWS S3 |
| **Database** | MySQL |
| **Monitoring** | Langfuse |

---

## 🔄 서비스 간 통신 흐름

```
┌──────────────┐      REST API       ┌──────────────┐      REST API      ┌──────────────┐
│              │ ──────────────────> │              │ ─────────────────> │              │
│   Frontend   │                     │   Backend    │                    │  AI Server   │
│   (React)    │ <────────────────── │   (Django)   │ <───────────────── │  (RunPod)    │
│              │      JSON Response  │              │    Rerank Result   │              │
└──────────────┘                     └──────────────┘                    └──────────────┘
                                            │
                                            │ API Calls
                                            ▼
                                    ┌───────────────┐
                                    │   External    │
                                    │   Services    │
                                    ├───────────────┤
                                    │ • Qdrant      │
                                    │ • OpenAI      │
                                    │ • Tavily      │
                                    │ • S3          │
                                    │ • MySQL       │
                                    │ • Mem0        │
                                    └───────────────┘
```

### 상세 흐름

```
[사용자]
    │
    ▼
[Frontend - React/Vite] ─────────────────────────────────────── 서비스 1
    │
    │ HTTP Request (REST API)
    ▼
[Backend - Django/DRF] ──────────────────────────────────────── 서비스 2
    │
    ├── agent_core 모듈 실행
    │       │
    │       ├── Qdrant 검색 (Vector Search)
    │       │       │
    │       │       ▼
    │       │   [RunPod AI Server] ─── HTTP ───────────────── 서비스 3
    │       │       │ (Reranker API)
    │       │       ▼
    │       │   (문서 재순위화 결과 반환)
    │       │
    │       ├── Tavily 웹 검색
    │       ├── Mem0 메모리 조회/저장
    │       └── OpenAI GPT-4o 호출
    │
    ▼
[응답 생성 → Frontend → 사용자]
```

---

## 🚀 배포 구성

| 서비스 | URL 예시 | 비고 |
|--------|----------|------|
| Frontend | `https://trade-ai.vercel.app` | Vercel 자동 배포 |
| Backend | `https://api.trade-ai.com` | AWS EC2 / ECS |
| AI Server | `https://xxx.runpod.net` | RunPod Serverless |

